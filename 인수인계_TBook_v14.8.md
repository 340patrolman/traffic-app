# T-Book(교통수첩) — 새 대화창 인수인계 (v14.8)

> 새 창에 **이 메모 + 최신 `index.html`**을 함께 올리면 맥락 없이 바로 이어집니다. (v14.7 인수인계를 대체)

## 0. 한 줄
서초서 교통 현장 보조 단일파일 웹앱(`index.html`, 약 2.7MB). 제작: 이동렬(교통안전1팀, 30년, INTJ, 정년 3년반). **현재 v14.8.** 앱명 「교통수첩 T-Book(티북)」. 후배 배포용 + 본인 실무용. "외우지 말고 펼쳐 보는 핸드북".

## 1. v14.8 변경 요약 (2026.7.7)
- 🇺🇸 **미국 경찰학 10장 신설** (아래 §5 상세)
- 📅 **제헌절(7/17) 공휴일 반영** — 2026년부터 재지정 확정(대체공휴일 대상), 언론 다수 교차확인(A~B급). 7월 기준시간 184→176h
- 💰 공휴일 추가/삭제(`addHoliday`/`removeHoliday`)에 `calcOT()` 추가 — 임시공휴일 편집 시 시간외 **즉시 재계산** (기존엔 계산값만 바뀌고 화면 미갱신 버그)
- 💰 **마이너스 시간외 표시** — 기준 미달 시 부족 h·역산 금액(−) 표시. 미니 라인·수당 카드 모두. 0h는 "정확히 충족" 별도 처리
- 🗓️ 달력 위 계급 select(`ot_rank2`) **삭제** — 단가 소스는 수당 카드 `ot_rank` 단일화(동기화 코드도 제거)
- 🐛 `_outside` **자정교차 패치** — 야간근무 중 자정 넘는 교육·동원 기록이 "근무 외"로 오판되던 버그. 윈도우를 +1440 한 번 더 대조하는 방식(`k*1440` 루프). 검증: 교육 23~01시(야간 중) → 차감 −2h ✓
- 📝 연가 차감 라벨 정확화 — "N일×8h" 오표기 → "자동차감 X.Xh(연가일 8h만 유급 인정)". **계산 로직 무변경**
- 버전 표기 8곳 v14.8 갱신, 역사 기록 3곳(27행 주석·구 체인지로그)은 v14.7 보존
- 🧹 **화면 통폐합 47→45**: ① `report_ref` 삭제 — `rep_manual`에 요약+상세(펼침) 합본, report 그룹 탭명 「📡 보고체계」로 변경, GROUP_PAGES·크로스링크 재지향 ② `det_voice` 삭제 — 텍스트 판별 통합 안내만 남은 껍데기(외부 참조 0 확인 후 제거). ※ `det_video`는 유령 아님 — 위반 분석 도구 살아있고 현장도우미(WZ)에서 진입, 유지. ※ 죽은 코드 잔존: `toggleDetectMic()`·`analyzeVoice()` 정의만 남음(호출처 0, 무해) — 다음 대청소 때 제거 후보
- 이모지 밀도 전수 측정: 평균 1.7개/1000자, 상위는 목록형(연락처·채증)이라 아이콘 기능 — 감축 실익 없음 판정, 형님 결정으로 보류

## 2. 배포 (변경 없음)
- `github.com/340patrolman/traffic-app` · Pages · https://340patrolman.github.io/traffic-app/
- 필수 파일: index.html, manifest.json, icon.png, icon-192.png, icon-512.png, sw.js

## 3. 편집 규율 (필수, 변경 없음)
- Python unique-substring 치환(count==1 assert)만 사용. 치환 후 구식별자 grep==0
- 매 수정 후 `node tests/test_tbook.mjs index.html` (tests/는 로컬 전용). ⚠️ v14.8은 공휴일 테이블 변경(제헌절)으로 골든 대조가 걸릴 수 있음 — 내용 확인 후 `--update` 판단
- 상단바 스타일은 문서 말미 「상단 버튼 최종 규칙」 블록에서만 수정

## 4. 교대근무표·수당 엔진
- SHIFT_SYS 4종(4x2/5x3/4x3/5x3s 반포 변형) — v14.7과 동일
- 수식: 총근무 − 평일수×8(기준) − 휴일주간×8(수당 별도) = 시간외 → cap → ×단가
- 연가 = 8h 유급 인정: lvAuto = Σ max(0, 근무시간−8) (박상권 제보, 코드 재현 검증 완료)
- ⚠️ **휴일 야간(holNS)은 미차감** — 휴일주간만 8h 차감. 의도인지 관서 기준인지 **박상권 7월 대조 시 확인 항목**
- 2026 공휴일 테이블 21개(제헌절 포함) 요일·대체공휴일 전수 대조 완료(A급)

## 5. 🇺🇸 미국 경찰학 콘텐츠 (v14.8 신설)
**시리즈 7부작** (읽는 순서 = 왜→말→몸→조직→회복→위기→정신위기, 상호 xlink):
①절차적 정의(`us_pj`, co_psych) ②Verbal Judo(`us_verbal`, co_excuse) ③오피서 세이프티(`us_safety`, sop_enforce) ④FTO 문서화(`us_fto`, co_staff) ⑤감정 생존(`us_gilmartin`, co_stress) ⑥위기 협상 BCSM(`us_bcsm`, co_excuse) ⑦정신위기 CIT(`us_cit`, co_psych)

**업무 보강 3장** (시리즈 번호 없음, 독립 카드):
NHTSA 음주 식별 단서(`us_dwi`, drunk — 상승기 판례 대응 관찰기록용) / 보고서 5원칙(`us_report`, rep_manual) / 법정 증언 3원칙(`us_court`, precedent)

**규칙**: 전 카드 각 페이지 맨 아래 접이식. 수입 원칙(개념만 수입, 법 근거는 한국법) 전문은 ① 카드에만, 나머지는 참조 1줄. 출처 전부 B급(서적·기관 자료) 병기.
**의도적 미이식(추가 금지)**: 무력사용 연속체 · Terry stop · 미란다 · 추격/PIT — 한국 법체계 충돌, 오용 위험. "못 찾은 게 아니라 안 넣은 것".

## 6. 수치 검증 현황
- ✅ 백색실선 전합 대법원 2022도12175 — 원문 대조 완료(A급)
- ✅ 제헌절 2026 재지정 — 교차확인 완료
- ⚠️ 시간외 단가(+3.5% 추정) — 2026 고시 원문 미확보, **경리계 확인이 최단 경로**. 확보 시 `ot_rank` 옵션 value 6개 수정(ot_rank2는 삭제됨 — 1곳만)
- ⚠️ 반포 변형 오프셋·시간 — 실물 순번표 확보 시 역산
- ⚠️ NHTSA 단서 확률 수치 — 참고치 성격(B급), 카드 내 명시됨
- 기존 미확정: 가속차로 조문 / 안전지대 벌점 / 음주 하급심 3건 / 올림픽대로 고시 / 복종의무 판례 번호

## 7. 사용자 컨텍스트 (변경 없음)
이동렬. 선결론·간결·반박·정확성(돈/법령/형사). 검증 채널: 박상권 반장(계산). 디자인: 예쁨보다 눈에 띔·실용.

## 8. 이어서 하기 좋은 것 (우선순위)
1. 시간외 단가 확정(경리계) → ot_rank 반영
2. 박상권 7월 대조 → 연가·수당 최종 확정 + **휴일 야간 미차감 정책 확인**
3. 반포 순번표 실측 → 5x3s 확정
4. 🇺🇸 카드 실기기 확인(분량·톤) + 후배 베타 반응
5. 확장 후보(보류): 없음 — 업무 축 공백 소진, 시리즈는 닫힌 완결
6. UI 잔여: 이모지 밀도, 화면 통폐합
