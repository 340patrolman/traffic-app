# T-Book 인수인계서 (HANDOFF) — ver.19.1
작성: 2026-07-27 / 대상: 새 대화창 첫 프롬프트에 이 파일 전체를 붙여넣을 것

---

## 0. 새 대화 시작 방법
새 창에서 이 파일과 `T-Book_v19.1.html`을 **함께 업로드**하고 이렇게 말하면 됩니다.

> 첨부한 HANDOFF_v19.1.md가 인수인계서다. T-Book_v19.0.html이 현재 최신본이다. 읽고 이어서 작업해라.

---

## 1. 프로젝트 정의
- **이름**: T-Book (교통수첩)
- **사용자**: 이동렬 경감 (서초경찰서 교통과 외근1팀, 30년 경력, INTJ)
- **형태**: 단일 파일 HTML PWA. 빌드툴 없음. 바닐라 JS.
- **현재 파일**: `T-Book_v19.1.html` (2,417,556 bytes / 48개 `<script>` 블록 / GROUP_PAGES 기반 **48페이지**)
- **배포**: 사용자가 직접 GitHub에 업로드. README 버전 표기 **v19.0**으로 갱신 필요.

---

## 2. 절대 변경 금지 (보안·법적 제약)
1. 진입 인증 게이트 (SHA-256, 5회 오답 잠금)
2. KILL 스위치 = 2027-02-20 만료 시 데이터 소거
3. 매뉴얼 AES-256-GCM 암호화
4. 평문 비밀번호·정답을 코드나 로그에 기록 금지
5. "남기는 글" 카드 — 사용자 명시 요청 없이 개고 금지
6. 데이터 수집·저장·외부전송 **0 원칙**
7. 외부 AI(Grok·Gemini) 평가는 **코드 검증 없이 채택 금지**
8. **계산식 등 로직 변경 전에는 반드시 먼저 묻는다** — 사용자 원문: *"뭘 바꾸기전에 물어보고 해라"*
9. **앱이 사용자 저장값을 바꿀 때는 무엇을 바꾸는지 화면에 공개해야 한다** — 사용자 원문: *"뭘 바꾸는지 알려줘야 한다"*
10. **클래식 홈을 제거하지 않는다** — 사용자 원문: *"나의 발자취도 있어야 하니"*. v19.0의 대시보드는 **선택제**이며 기본값은 클래식이다.

---

## 3. 작업 파이프라인 (그대로 따를 것)
1. Python 문자열 치환 패치. **반드시** `assert s.count(old)==1` 가드.
2. `<script>` 블록 추출 → `node --check` 전수 통과 확인 (현재 48개).
3. Playwright 렌더 + 48페이지 pageerror 스윕 → 0건 확인. **dash / classic 두 모드 모두** 돌릴 것.
4. **화면 변경 시**: 여러 해상도 × 여러 달 실측.
5. `SendUserFile`로 전달.

### 환경
- Chromium: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
- 앱이 `file://`을 차단하므로 `python3 -m http.server 8901`로 서빙
  (`(setsid nohup python3 -m http.server 8901 >/dev/null 2>&1 < /dev/null &)`)
- 모달 억제 localStorage 키: `intro_hide_v141`, `entry_gate_ok`, `police_v6_accepted`, `tbook_tour_done`, `pwa_modal_closed_time`
- **v19 전용 키**: `tbook_home_mode` = `'dash'` | `'classic'` (미설정 = classic)
- 페이지 전환은 `showGroup(g)` → `showSubPage(id)` (GROUP_PAGES **12그룹 48페이지**)

### 작업용 스크립트 (재사용 권장)
| 파일 | 용도 |
|---|---|
| `patch_v19.py` | v18.9 → v19.0 전체 패치 (멱등, 소스에서 재생성 가능) |
| `sweep19.py <dash\|classic>` | 48페이지 pageerror + 상단바 1줄 유지 검증 |
| `audit.py` / `audit2.py` | 전수 시각 점검 (가로넘침·글자잘림·터치타겟) |
| `measure.py` | 해상도 × 12개월 달력 셀 높이 실측 |
| `shot.py` | 단일 화면 스크린샷 |

---

## 4. v19.0에서 한 일 — **내용에서 사용성으로**

시작점: 사용자가 *"윗부분 버튼들을 한 줄로 만들어서 자료를 한번에 많이 보이게"*, 이어서 *"상용앱처럼 매끄럽고 우아하지만 사용성·현장성이 있는 모습"* 을 요구.

**계산 로직은 v18.5 이후 단 한 줄도 바뀌지 않았다. 전부 표현·배치 작업이다.**

### 4-1. 상단바 1줄화 (141px → 57px)
- 마크업 교체: `.topbar` → `.topbar.tb1`, `.topbar-btns` 컨테이너 **제거**(이걸로 기존 19개 규칙이 자동 무력화됨)
- 구성: `🚔 T-Book` `◀` — 여백 — `☰ 메뉴` `매뉴얼` `🔎 찾기` `⚙️ 설정`
- 🌙 낮밤 → 설정 「🌗 화면 모드 · 낮/밤」(`tsetA2b`)로 이동. **`id="theme_btn"`을 그대로 옮겨서** `applyTheme()`의 아이콘 동기화가 유지됨.
- 🔊 읽기 → 삭제. 설정에 이미 「🔊 읽어주기」(`tsetA3`)가 있었음.

**⚠️ 반드시 알아야 할 함정 — 어제 화면이 깨진 원인이 이것이다**
```
@media(max-width:520px){
  .topbar{flex-wrap:wrap!important; ...}                    ← 강제 2줄
  .topbar>div:first-child{flex:1 1 100%!important;
                          min-width:100%!important}          ← 제목이 한 줄 독점
}
```
마크업만 한 줄로 바꾸면 이 두 줄이 그대로 살아서 **다시 2줄로 되돌리거나 버튼을 뭉갠다.**
v19에서는 두 규칙을 **`.topbar:not(.tb1)`** 로 좁혀서 차단했다. 상단바를 다시 건드릴 때 이 처리를 지우면 똑같이 깨진다.

### 4-2. 홈 대시보드 (선택제)
- 새 그룹 `home` + 새 페이지 `home_dash` 신설 (GROUP_PAGES 맨 앞)
- `['report','docs',...]` 하드코딩 그룹 배열이 **2곳**(2417줄·5388줄) 있음 → 둘 다 `'home'` 추가함
- 구성: ① `오늘` 근무·시간외 카드 → ② `바로 실행` 6칸 → ③ `최근 본 항목`
- **바로 실행 순서는 사용자 지정**: 교대근무표 · 서류작성 · 현장심리 · 교통관리 · 사고처리 · 보고서
  상위 2개(매일 씀)는 큰 타일, 하위 4개는 작은 타일 — **손이 갈 확률을 크기로 표현**
- `renderHomeDash()`는 **`_monthCalc()` 결과를 읽기만 한다.** 계산 무변경.
  사용자가 보던 달을 건드리지 않으려고 `_shiftView`를 임시로 이번 달로 바꿨다가 `finally`에서 복원한다.
- `최근 본 항목`은 기존 `usage_counts` localStorage를 재사용 (외부 전송 없음)

### 4-3. 하단 고정 탭바 5개 (대시보드 모드 전용)
`홈` · `현장`(acc_hub) · `매뉴얼`(doc_manual) · `연락처`(expr_contact) · `전체`(vnavToggle)
- `body[data-home="dash"]` 일 때만 표시. 클래식 모드에서는 CSS 한 줄도 적용되지 않는다.
- 대시보드 모드에서는 **상단 그룹탭(`.nav-tabs`)을 숨긴다** — 하단 탭바와 역할이 겹침
- `body` 하단 여백 96px + safe-area 확보
- 하단 고정 요소들(`#toast`, `#more_below_hint`, `*_fab`)을 탭바 위로 올림 → **겹침 0건 검증 완료**

### 4-3-1. ⚠️ 전체 메뉴 — 초판 결함과 수정 (반드시 읽을 것)
v19.0 초판에서 대시보드 모드일 때 **☰메뉴·하단 '전체'를 눌러도 아무것도 안 떴다.**
원인: 내가 넣은 `body[data-home="dash"] .nav-tabs{display:none!important}` 가
기존의 `body.vopen[data-nav="v"] .nav-tabs{display:flex}` 를 `!important`로 눌러버린 것.
```css
body[data-home="dash"] .nav-tabs{display:none!important}          /* 평소 숨김 */
body[data-home="dash"].vopen .nav-tabs{display:flex!important}    /* ← 이 줄이 없으면 먹통 */
```
추가 조치:
- 상단 ☰메뉴 버튼과 하단 '전체' 탭을 모두 **`hdMenu()`** 경유로 통일.
  기존 `vnavToggle()`은 「메뉴 보기 방식=가로형」일 때 `#vnav_menu_btn`이 `display:none`이라 눌리지 않았다.
  `body[data-home="dash"] #vnav_menu_btn{display:inline-flex!important}` 로 대시보드에서는 항상 보이게 했다.
- 기존 `showGroup` 후킹은 `_navMode()==='v'` 일 때만 `vopen`을 지운다 → 가로형에서 메뉴가 안 닫혔다.
  v19 스크립트에서 `showGroup`을 한 번 더 후킹해 **모드와 무관하게 `vopen` 제거**.
- 열린 메뉴는 11개 그룹을 **2열 격자**로 표시 (세로 나열이면 852px로 화면을 넘긴다 → 463px로).

**검증 (4조합 전수)**
| 모드 / 메뉴설정 | 메뉴 클릭 후 | 그룹 선택 후 |
|---|---|---|
| dash / 세로형 | 463px 펼침 ✅ | 닫힘 ✅ |
| dash / 가로형 | 451px 펼침 ✅ | 닫힘 ✅ |
| classic / 세로형 | 855px 펼침 ✅ | 닫힘 ✅ |
| classic / 가로형 | 항상 표시(기존 동작) ✅ | — |

### 4-4. 설정 「🏠 홈 화면 방식」 (`tsetHM`)
- **클래식** / **대시보드** 를 미리보기 그림과 함께 선택. **기본값 = 대시보드 (v19.1에서 사용자 지정으로 변경)**.
  `hdMode()`는 저장값이 `'classic'`일 때만 클래식, 그 외(미설정 포함)는 `'dash'`.
- `hdSetMode(m)` → `localStorage['tbook_home_mode']` 저장 + 즉시 반영
- T-Book 로고(`goHome()`)는 선택한 모드의 홈으로 간다

### 4-5. 서류매뉴얼 표 잘림 수정 [실제 결함]
`서류매뉴얼 → 🌐 외국인 위반 단속`의 표 3개가 **619px**까지 늘어나 화면(412px) 밖으로 잘리고, 가로 스크롤도 죽어 있어 오른쪽을 볼 방법이 없었다.
```
173줄  .tbl-scroll{overflow-x:auto}      ← 원래 의도
3708줄 .tbl-scroll{overflow-x:visible}   ← 나중에 와서 덮어씀 = 스크롤 사망
       + td{white-space:nowrap} 이 겹쳐 표가 늘어남
```
→ `#page-doc_manual` 한정 규칙으로 셀 줄바꿈 허용 + `table-layout:fixed`. **619px → 313px.** 다른 페이지 영향 없음.
**주의**: 이 `overflow-x:visible` 규칙은 전역으로 아직 살아 있다. 다른 페이지의 넓은 표에서 같은 문제가 재발할 수 있다 → 7-2 참조.

### 4-6. 발자취 (`tsetA9`)
「📜 개발 이력」 → **「🐾 발자취 — 이 앱이 걸어온 길」** 로 개명. v18 시대 + v19.0 항목 추가(기본 펼침).

---

### 4-7. v19.1 변경분 (사용자 지시)
- **기본 홈 = 대시보드.** 클래식은 설정에서 선택(제약 10 유지 — 제거하지 않음).
- **하단 탭 5칸 확정**: `홈`(home_dash) · `서류`(docs/checklist 작성순서) · `매뉴얼`(docs/doc_manual) · `찾기`(openSearch) · `전체`(hdMenu)
  서류·매뉴얼이 같은 `docs` 그룹이라 **활성 판정을 그룹이 아닌 페이지 기준**으로 바꿨다 (`TAB_PAGE`/`TAB_GROUP`).
  `showSubPage` 후킹에서 `document.body.setAttribute('data-page', pid)` 를 심는다 — 아래 상단바 규칙이 이걸 쓴다.
- **상단바**: 대시보드 모드에서 `매뉴얼`·`찾기` 버튼 제거(하단으로 내려감) → 로고·◀·☰메뉴·⚙️설정 4개, 48px.
  **홈(`home_dash`)에서는 상단바 자체를 숨긴다** → `body[data-home="dash"][data-page="home_dash"] .topbar{display:none!important}`
  홈에서 설정은 '오늘' 줄 오른쪽 톱니(`.hd-gear`)로 들어간다. **홈에서 전체 메뉴는 하단 '전체' 탭으로만 연다**(상단 ☰가 없으므로).
- **떠 있는 버튼 반투명**: `#fabAll`(▾ 전부 펼치기)·`#more_below_hint`(아래 내용 더 있음) → `opacity:.42` + blur, 누르면 1.0.
  ⚠️ 실제 id는 `fabAll` 이다. `[id$="_fab"]` 로는 안 잡힌다.

**v19.1 검증**: 48페이지 × (기본/클래식) pageerror 0 · 상단바 이슈 0 · 고정요소 겹침 0 · 스크립트 구문오류 0/48
메뉴 열기 4조합(기본·클래식 × 세로형·가로형) 전부 정상, 하위 페이지 상단 ☰도 동작.

---

## 5. 계산식 (변경 금지 — 검증 완료)
```
총 근무 = Σ(근무일수 × 1회 근무시간) + 근무외가산 + 추가 − 연가/조퇴/지각
기준초과 = 총근무 − (평일수 × 8h)
시간외   = 기준초과 − (휴일주간일수 × 8h)
```
근거: 공무원수당 등에 관한 규정 §15(시간외) / §16(야간) / §17(휴일)

### 5-1. 연가 자동차감 (`lvAuto`)
```js
lvAuto = lvD*max(0, dh-8) + lvN*max(0, nh-8) + lvM*max(0, mh-8) + lvA*max(0, ah-8)
```
연가일은 **8h만 유급 인정**. 야간 연가 1일 = 13.5 − 8 = **5.5h 차감**. 3일이면 16.5h.

### 5-2. §16·§17 앱 반영 상태
| 규정 | 앱 반영 |
|---|---|
| §17 휴일수당 시간외와 **병급 불가** → 차감 | ✅ `시간외 = 기준초과 − 휴일주간일수×8h` |
| §16 야간수당 시간외와 **병급 가능** → 차감 안 함 | ✅ 야간 13.5h 전액 포함 |
| §16 금액(봉급기준액 × 1/209 × 50%) | ❌ 미포함 (의도적, 원문 A등급 확정 전) |
| §17 금액(봉급기준액 × 1/26 × 150%) | ❌ 미포함 (의도적) |

**검산 [A등급]**: 경위 12,300원/h 기준 휴일수당 1일 98,873원 vs 같은 8h 시간외 98,400원 → **+473원**. 1/26 ≈ 8/209 라 산식상 거의 등가.

**공휴일 효과 [A등급]**: 공휴일 휴무·비번 → 기준만 −8h → **시간외 +8h 순증**. 공휴일 주간근무 → 기준 −8h와 휴일주간 −8h가 상쇄 → 평일과 동일, 대신 휴일수당 1일분 별도.

---

## 6. 검증 결과 (v19.0)

### 6-1. 계산 정확성 [신뢰도 95 | A등급 — 관서 공식 근무지정표 전수 대조]
- **2026-06 (1팀)**: 주7·야7=175h, 평일 21일 → 기준 168h → 기준초과 +7h → 휴일주간 3일 −24h → **−17h** (지정표 일치)
- **2026-07 (1팀, 연가 없음)**: 200h → +24h → −24h → **0h · 0원**
- **2026-07 (야간 연가 3일)**: 183.5h → +7.5h → −24h → **−16.5h · 0원** (실기기 재현 일치)
- 2팀·4팀은 지정표 합계란이 자기 날짜칸과 불일치(자원근무·선거일 추정). **앱은 주기 칸을 따른다.**
- 제헌절 2026년 공휴일 재지정 — `'2026-07-17':'제헌절'` 정확

### 6-2. 코드 품질 게이트
| 항목 | 결과 |
|---|---|
| `<script>` 블록 구문오류 | **0 / 48** |
| 48페이지 pageerror (dash 모드) | **0** |
| 48페이지 pageerror (classic 모드) | **0** |
| 상단바 2줄 복귀·가로넘침 | **0** |
| 글자 잘림 | **0** |
| 실질 가로 넘침 | **0** (v18.9의 doc_manual 1건 수정 완료) |
| 하단탭 ↔ 고정요소 겹침 | **0** |

### 6-3. 달력 셀 높이 (상단바 1줄화 효과) [A등급, 7해상도 × 12개월 실측]
| 해상도 | v18.9 | v19.0 | 증가 |
|---|---|---|---|
| 360×640 | 53~66 (**여백 −8 넘침**) | 59~87 (여백 +26~31) | **넘침 해소** |
| 360×780 | 73~95 | 78~116 | +19.8% |
| 390×844 | 74~111 | 88~132 | +19.2% |
| 412×915 | 86~129 | 100~150 | +15.7% |
| **412×1011 (사용자 기기)** | **102~153** | **115~174** | **+13.7%** |
| 430×932 | 88~132 | 103~154 | +16.3% |
| 768×1024 | 112~169 | 117~176 | +4.4% |

글자가 셀 밖으로 나가는 경우 **전 조합 0건**. HANDOFF v18.9에서 "해결 불가"로 판단했던 **360×640 넘침이 상단바 1줄화로 자동 해결**됐다.

---

## 7. 남은 과제 (우선순위 순)

### 7-1. 터치 타겟 — **미결, 사용자 결정 필요**
손가락 권장 44px 미만인 곳:
| 화면 | 개수 | 높이 |
|---|---|---|
| `co_excuse` 현장대응멘트 | 74 | 27px (해시태그 칩) |
| `precedent` 판례 | 33 | 30px |
| `co_psych` 현장심리 | 8 | 33px |
| `doc_manual` 서류매뉴얼 | 7 | 36px |
| `shift_cal` 교대근무표 | 5 | 19~32px |

**충돌**: 44px로 키우면 한 화면에 들어가는 항목 수가 줄어든다 — 사용자가 원하는 "한 번에 많이 보이게"와 정면 충돌.
**제안한 절충안**: *누르는 것만* 44px로 키우고 *읽기만 하는 라벨*은 그대로. 해시태그 칩이 실제 클릭 대상인지 표시용인지 확인 후 결정할 것.

### 7-2. `.tbl-scroll{overflow-x:visible}` 전역 잔존
3708줄의 이 규칙이 173줄의 `overflow-x:auto`를 계속 덮고 있다. doc_manual만 우회했으므로 **다른 페이지의 넓은 표에서 재발 가능**. 근본 수정(규칙 제거 또는 `:not()` 한정)은 영향 범위가 넓어 **사용자에게 먼저 물을 것**(제약 8).

### 7-3. 8.7 발표 D-11 — 법령 재검증
도로교통법 시행령 **별표8·별표10** 개정 여부. 시행령이 2026-02-19 대통령령 제36114호로 개정된 것은 확인, 별표 내용 변동은 **미확인**.

### 7-4. §16 야간근무수당 원문 A등급 확정
현재 **B등급**: 야간수당 = 봉급기준액 × 1/209 × 50%. `law.go.kr` 직접 접속이 robots.txt로 차단되어 2차 출처(nepla)로만 확인. **원문 대조 필요.**
확정되면 **야간수당 추정 행을 정산표에 추가할지 먼저 물을 것**(제약 8).

### 7-5. 외부 AI 위임 지시문
**Grok** (실시간·속보):
```
1. 대한민국 도로교통법 시행령이 2026-02-19 대통령령 제36114호로 개정되었다.
   이 개정 및 2026년 내 다른 개정에서 별표8(범칙행위 및 범칙금액)과
   별표10 금액·항목 변동이 있었는지, 2026-08-07 이전 시행 예정인
   개정·입법예고가 있는지 최신 뉴스·공보 기준으로 알려달라.
2. 음주측정거부(도로교통법 §148조의2) 및 영장 없는 강제채혈 관련
   2025~2026년 대법원 선고·보도 판례를 사건번호와 함께 목록화해달라.
※ 각 항목에 출처 링크 필수.
```
**Gemini** (원문·대용량·이미지):
```
1. 국가법령정보센터(law.go.kr)에서 「공무원수당 등에 관한 규정」(현행)
   제15조·제16조·제17조 조문 원문을 한 글자도 바꾸지 말고 그대로 인용해달라.
   특히 §16 야간근무수당의 시간당 산정식을 확정할 것.
2. [수위표 공문 스캔본 첨부 시] 첨부 공문 원문과 전사본을 대조해
   수치·단위·조건 불일치를 전부 표로 나열해달라.
```

### 7-6. 기타
- **수위표 공문 원문 대조** (현재 B등급). 내부 문서라 외부 AI 입수 불가, 사용자가 원본 스캔 확보 필요.
- GitHub 업로드(사용자 직접) + README 버전 **v19.0** 갱신
- 월간 유지보수 스케줄 가동 중: `trig_01EJkqoLdxERkspV16xqLTDZ`, cron `0 0 1 * *` UTC (매월 1일 09:00 KST)

---

## 8. 디자인 원칙 (v19.0에서 확립 — 이후 화면 작업 시 따를 것)
1. **이모지 대신 단색 라인 SVG.** 이모지는 기기·OS마다 그림이 달라 "만든 티"가 난다. 상용/비상용을 가르는 가장 큰 단일 요소.
2. **색은 의미로만.** 금=근무·수당 / 청=기록·문서 / 녹=대화·심리 / 주황=통제·단속 / 적=긴급·사고 / 회색=저빈도. 장식용 색 금지.
3. **색의 세기도 우선순위다.** 빈도 낮은 항목(보고서)에 강한 색을 주면 눈이 그리로 끌려간다.
4. **카드 본체는 중립, 색은 좌측 4px 띠와 아이콘 배경에만.** 카드 전체를 칠하면 6색은 무너진다.
5. **타이포 3단계 고정** (22 / 16 / 12px), **라운드 16px 통일**, **간격 9~10px 통일**. 고급스러움은 색이 아니라 반복되는 규칙에서 나온다.
6. **그림자 대신 1px 경계선.** 그림자 남발은 아마추어 신호.
7. **야간 모드는 순검정 금지** — `#000` 대신 `#0d1320` 계열.
8. **아이콘만 남기고 글자 라벨을 지우지 않는다.** 예뻐지지만 급할 때 못 찾는다.
9. **터치 타겟 최소 44px**(장갑 착용 시 56px 권장), 대비 4.5:1 이상(햇빛 아래 가독).

---

## 9. 응답 규약 (사용자 개인 설정 요약)
Strategic Advisor v3.2 Pro Edition 적용. 핵심만:
- 한국어. 선결론 우선. 의례적 사과·추임새·아첨 금지.
- 신뢰도 표기 50·60·70·80·90·95 단위, 근거 A/B/C 등급 병기(분쟁 가능 시).
- **반박 의무**: 전제·논리 허점 발견 시 즉시 지적. 단 동의를 구하는 맥락에선 검증 후 합리적 인정 허용.
- **점검하지 않은 것을 점검했다고 말하지 않는다.** (v19.0 작업 중 실제로 이 지점에서 정정한 사례 있음)
- 정량 데이터(날짜·법조문·통계)는 **코드 실행으로 직접 검증** 후 답변. 메모리 의존 금지.
- 재산·형사·건강·가족 사안은 말미에 "전문가 교차검증 권고" 1줄 필수.
- 토큰 한도 80% 도달 시 미리 알릴 것.

---

## 10. 현재 상태 한 줄 요약
**v19.0 완성·검증·전달 완료. 계산 로직은 v18.5 이후 무변경(6월 −17h / 7월 0h / 연가 반영 −16.5h 모두 지정표·실기기와 일치). v19.0은 전부 사용성 작업 — 상단바 1줄(141→57px, 달력 셀 +14%), 홈 대시보드와 하단 탭바 신설(선택제, 기본값은 클래식), 서류매뉴얼 표 잘림 결함 수정. 48페이지 × 2모드 오류 0건. 다음은 터치 타겟 결정(7-1), 8.7 발표용 별표8·별표10 재검증(7-3), §16 원문 A등급 확정(7-4).**
