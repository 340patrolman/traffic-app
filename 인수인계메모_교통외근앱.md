# 교통외근 통합보조 앱 — 인수인계 메모 (이사용)

새 대화창에 **이 메모 + `index.html`** 을 함께 올리면 맥락 없이 바로 이어집니다.
(아이콘/배포는 `icon.png`, `robots.txt`, **`sw.js`(v10.3 신규)** 도 같은 폴더에 있음)

---

## 0. 한 줄 요약
서초경찰서 교통과 현장 보조 **단일 파일 웹앱**(`index.html`, **v10.3 기준 약 2.3MB** — 이전 6.4MB에서 업무매뉴얼 텍스트화로 감축). 본인(이동렬, 교통안전1팀)이 직접 제작·배포. **이미 정식 배포됨 → https://340patrolman.github.io** (GitHub Pages). 외부 전송 0, 모든 데이터 localStorage. **v10.3부터 PWA 설치(홈 아이콘) 정상 작동 + 오프라인 캐시(서비스워커 `sw.js`).**

## 1. 배포 구조 (GitHub Pages)
- 주소: **https://340patrolman.github.io**
- 같은 폴더(루트)에 올려야 하는 파일: **`index.html`**, **`icon.png`**(아이콘·카톡썸네일), **`robots.txt`**(검색차단), **`sw.js`(v10.3 신규 — 빠지면 설치·오프라인 둘 다 작동 안 함)**
- 수정 = `index.html` 교체 업로드 → 사용자는 다음에 열 때 자동 반영 (서비스워커가 **네트워크 우선**이라 온라인이면 항상 최신본을 받음)
- 검색 차단: `<meta robots noindex>` + robots.txt 적용됨 (내부 공유 전용)
- 카톡 미리보기: og 태그 + og:image=`https://340patrolman.github.io/icon.png` 설정 완료
- CSP에 `manifest-src 'self' blob:; worker-src 'self';` 추가됨(v10.3) — 이게 없으면 매니페스트가 막혀 안드로이드 설치 프롬프트가 안 뜸.

## 1.5. ★ PWA 설치 / 주소 숨김 / 원격회수 — 주의 (v10.3 핵심 변경)
- **설치 = 주소 숨김:** 크롬/사파리에서 링크를 열고 "앱 설치/홈 화면에 추가" → `display:standalone`이라 **주소표시줄(github.io)이 사라짐**. 카톡으로 받아 링크로 열어 설치하면 됨. iOS는 자동버튼 미지원 → Safari 공유(↑)→"홈 화면에 추가".
- **안드로이드 네이티브 설치 프롬프트는 서비스워커(`sw.js`)가 있어야 뜸.** 그래서 v10.3에서 SW를 다시 넣음.
- **⚠️ v9.1과의 충돌:** v9.1에서 "원격 회수(킬 스위치)"를 위해 SW를 일부러 제거했었음. v10.3에서 설치 아이콘 요구로 SW를 재도입 → **순수 페이지 삭제로는 회수가 안 됨**(기기 캐시로 오프라인 잔존).
  - **회수 방법(권장):** github 페이지를 **삭제하지 말고**, index.html을 "사용중지+데이터 삭제" 페이지로 **교체**할 것. SW가 네트워크 우선이라 기기가 온라인이 되면 교체본을 받아 실행됨.
  - 기기별 수동 완전 삭제는 설정의 **🧨 완전 초기화** 버튼(localStorage·캐시·SW 전부 제거).
  - 하드 킬스위치(페이지 삭제만으로 회수)가 더 중요하면 SW를 빼야 함 → 그러면 안드로이드 설치 아이콘은 포기(iOS·바로가기만 가능).

## 2. ★ 시간외 계산 공식 (검증 완료 — 가장 중요)
공식 6월 근무지정표(4개 팀)로 검증함.

```
법정근무시간(복무규정) = 그 달 평일수 × 8   (주말·공휴일 제외, 2026-06-03 지방선거 포함 → 6월=21평일=168h)
주간 1회 = 11.5h, 야간 1회 = 13.5h

work(총근무) = 주간횟수×11.5 + 야간횟수×13.5 + 날짜기록(±) + 추가근무
               − 연가일수×8 − 조퇴h − 지각h
기준초과근무시간 = work − 복무규정
시간외(sigot)   = 기준초과 − 휴일주간×8      ← 음수 그대로 표시(클램프 X)
야간시간(표시)  = 야간횟수 × 8
```
- **휴일주간** = 토/일/공휴일에 "주간" 근무한 일수(자동 감지). 8h는 휴일근무수당 별도지급분이라 시간외에서 공제.
- **연가** = 1일당 −8h (정액). 연가일은 날짜기록 가산 무시(continue).
- **날짜기록 물리력·사격·직장훈련·교육** = 근무시간과 **겹치는 시간은 −차감 / 밖은 +가산**(overlap 계산, `_outside`). **동원**은 밖 가산만. **주간자원 +9 / 야간자원 +10 / 아침러시·저녁러시 각 +2**.
- 근무시간대: 주간 06:30~18:00, 야간 17:30~익일07:00.

**검증값(2026.6, 깨끗한 상태):** 1팀(주7·야7·휴일주간3) = 시간외 **−17**, 기준초과 7, 야간 56 / 3팀(주8·야8·휴일주간2) = 시간외 **+16**, 기준초과 32, 야간 64 / 2팀 총근무 **186.5h**.

## 3. 교대 패턴
- `SHIFT_PAT=['주간','야간','휴무','비번']`, 순환 **주→야→휴→비**
- `_stateIdx`: `((diff + anchorIdx + (team-1))%4+4)%4`, team 1~4 오프셋 +1씩
- 2026-06-01 기준: 1팀=휴, 2팀=비, 3팀=주, 4팀=야 (이 관계와 앱이 일치 확인됨)
- 사용자가 "내 팀 + 오늘 상태" 지정 → anchor 저장(`shift_myteam`, `shift_anchor_date`, `shift_anchor_idx`)

## 4. 주요 함수 / 위치
- `_monthCalc()` 월 계산(work/gijun/sigot/nightHrs/holDS 등 반환)
- `calcOT()` 결과+📐계산식 표시
- `renderShiftCal()` 달력 + 상단 요약 + 🔄새로고침 버튼 + 📊내역(±) 줄
- `_outside(s,e,wins)` 근무시간 겹침/밖 계산, `_weekdaysInMonth()` 평일수
- `openDayNote()/saveDayNote()` 날짜기록 팝업(시간입력 5종 + 체크 항목)
- `HOLIDAYS_BASE` 공휴일(2026 포함, 6/3 지방선거 포함)
- `loadTheme()` 기본 밝은화면(시스템 다크 추종 제거), `toggleTheme()` 🌙/☀️
- 전역검색 `openSearch()`, 사용량 `usage_counts`
- **`clearAll()`** 입력만 초기화(설정·백업 유지) / **`factoryReset()`(v10.3 신규)** 전체 데이터+캐시+SW 삭제 후 reload
- **`_buildGallery()`** — 업무매뉴얼 갤러리. v10.3부터 배열 요소가 `"html:..."` 면 텍스트 카드(.mp), 아니면 base64 이미지로 렌더.

## 5. 메인 탭 순서
사건보고 · 서류작성 · 교대근무표 · 단속판별 · 현장SOP · 대응코칭 · 자동차전용도로 · 참고자료 · 유관단체

## 6. ★ 기술 주의사항 (편집 시 필수)
- **단일 파일** — 수정은 **Python 스크립트 권장**(Edit 도구로 끝 잘린 사고 있었음), 끝에 `</html>` 유지 확인 필수.
- **업무매뉴얼 구조(v10.3 변경):** 더 이상 통짜 이미지 PDF가 아님. 복호화하면 **JSON 배열 21요소** = 텍스트 14장(`"html:"` 접두 HTML 문자열) + 이미지 7장(base64 JPEG). 양식·PC/폰 화면 캡처(p4·p14·p15·p16·p17·p19·p20, 1-based)는 이미지 유지.
- **암호화:** `PDF_ENC`(wraps 2개=정답 **355 / 395** 둘 다 유효, PBKDF2 60만회, AES-256-GCM) + `PDF_DATA_B64`. v10.3 재암호화 시 **동일 키 재사용 + `data_iv`만 새로 생성**했으므로 비번 355/395 그대로. 매뉴얼 내용 수정 시 같은 방식(복호화→배열편집→동일 K로 재암호화→`data_iv`·`PDF_DATA_B64` 교체) 필요. 잠금 질문은 "센터 경비 전화번호는?".
- **매뉴얼 텍스트 CSS:** `<style id="mp-style">`의 `.mp-*` 클래스(.mp-title/.mp-t 표/.mp-box 빨강박스/.mp-note 노랑주석/.mp-quote).
- JS 문자열 일부 구역에서 가운뎃점·마이너스를 유니코드 이스케이프로 저장 → 문자열 매칭 시 주의.
- **검증 루틴:** 수정 후 ①`<script>` 블록 `node --check` ②`<div>`/`<script>` 여닫기 균형 ③`</html>` 종료 ④돈 계산은 모의 시뮬레이션으로 검증값(1팀 −17 등) 대조 ⑤암호화 변경 시 355·395 둘 다 복호화·JSON 파싱 확인.

## 7. 최근 완료
- **(v10.3 · 2026.6.16)** 📲 PWA 설치 정상화(서비스워커 `sw.js` 추가, CSP manifest-src 수정, 우하단 📲설치 버튼, 설치 시 주소 숨김), 현장 오프라인(네트워크 우선). 🗜️ 업무매뉴얼 14장 텍스트화 → **앱 6.5MB→2.3MB(−65%)**. 🧨 완전 초기화 버튼 신설.
- (그 이전) 시간외 계산 공식 재구현(부호 그대로), 연가 −8 정액, 날짜기록 근무시간 내 −차감/밖 +가산, 휴일주간 자동공제, 🔄새로고침·📊내역(±)·소수(.5) 표시, 다크모드 기본 밝게, 첫화면 압축, 전역검색·자주쓰는화면, 혹서기 근무지침, noindex/robots, icon.png+og태그.

## 8. 대기/후보 (미착수 또는 검증 대기)
- [ ] **폰에서 v10.3 실측 확인:** 안드로이드 크롬 "앱 설치" 프롬프트 실제 표출 / 설치 후 주소 숨김 / 오프라인 작동 / 📲설치 버튼 / 🧨완전 초기화.
- [ ] **변환된 매뉴얼 14장 법조문·절차 텍스트 검수** (AI 전사분 — 형님 원문 대조 필수). 원본 이미지는 보존되어 있으니 오탈자 발견 시 해당 페이지만 이미지로 되돌리거나 텍스트 수정.
- [ ] SW vs 원격회수 정책 결정(§1.5) — 하드 킬스위치 유지 vs 설치 아이콘 유지.
- [ ] 2팀/4팀 공식 정확값 대조 / 휴일근무수당 별도 단가 미반영 / 혹한기 근무지침.

## 9. 사용자(형님) 컨텍스트
- 이동렬, 서초서 교통안전1팀, 30년 경력. INTJ. 깊이·정확성·반박·토큰절약 선호, 한국어.
- 앱 목적: 인사이동(50% 교체) 신임·전입 직원 빠른 전력화 + 매일 쓰는 도구. 현장 의견 즉시 반영이 강점.
- 돈(시간외수당) 관련은 정확성 최우선 — 추측 금지, 검증값 대조.
