# T-Book 인수인계서 (HANDOFF) — ver.19.4
작성: 2026-07-28 (ver.19.3 → 19.4 갱신)

> **v19.4 변경점 3줄**
> ① 도로교통법 시행령 별표8·별표10 원문 재검증 완료 — 무변동 확정, 7-2 과제 종결.
> ② 이륜차 불법 주·정차 과태료 입법 공백을 별표6·7 원문으로 확인해 앱에 반영(입법예고 미시행 명시).
> ③ 법령DB 기준일 2026.4 표기에 재확인 범위를 병기해 앱 내 기준일 불일치 해소.

작성일 기준: 2026-07-28 / 대상: 새 대화창 첫 프롬프트에 이 파일 전체를 붙여넣을 것

---

## 0. 새 대화 시작 방법
새 창에서 이 파일과 `T-Book_v19.3.html`을 **함께 업로드**하고 이렇게 말하면 됩니다.

> 첨부한 HANDOFF_v19.3.md가 인수인계서다. T-Book_v19.3.html이 현재 최신본이다. 읽고 이어서 작업해라.

---

## 1. 프로젝트 정의
- **이름**: T-Book (교통수첩)
- **사용자**: 이동렬 경감 (서초경찰서 교통과 외근1팀, 30년 경력, INTJ)
- **형태**: HTML PWA. 빌드툴 없음. 바닐라 JS.
- **현재 파일**: `T-Book_v19.4.html` (약 2,450,000 bytes / `<script>` 블록 **52개** / GROUP_PAGES 기반 **48페이지**)
- **저장소**: `github.com/340patrolman/traffic-app` (main 브랜치) / **라이브**: `https://340patrolman.github.io/traffic-app/`
- **배포 구성 — 단일 파일이 아니다**: 저장소에 `index.html` · **`sw.js`** · `manifest.json` · `icon.png` · `icon-192.png` · `icon-512.png` · `robots.txt` 가 **모두 이미 존재한다**(2026-07-27 확인).
- ⚠️ **`sw.js`를 새로 만들거나 덮어쓰지 말 것.** 사용자가 이미 v2로 다듬어 놓았고, 그 설계가 더 낫다 — 4-2 참조.
- README 버전 표기 — 저장소 README는 2026-07-28 확인 시점에 **v18.1**로 두 판 밀려 있었다. v19.4로 갱신할 것.

---

## 2. 절대 변경 금지 (보안·법적 제약)
1. 진입 인증 게이트 (SHA-256, 5회 오답 잠금)
2. KILL 스위치 = 2027-02-20 만료 시 데이터 소거
3. 매뉴얼 AES-256-GCM 암호화
4. 평문 비밀번호·정답을 코드나 로그에 기록 금지
5. "남기는 글" 카드 — 사용자 명시 요청 없이 개고 금지
6. 데이터 수집·저장·외부전송 **0 원칙** (CSP `connect-src 'none'` 으로 강제되어 있음)
7. 외부 AI(Grok·Gemini) 평가는 **코드 검증 없이 채택 금지**
8. **계산식 등 로직 변경 전에는 반드시 먼저 묻는다** — 사용자 원문: *"뭘 바꾸기전에 물어보고 해라"*
9. **앱이 사용자 저장값을 바꿀 때는 무엇을 바꾸는지 화면에 공개해야 한다** — 사용자 원문: *"뭘 바꾸는지 알려줘야 한다"*
10. **클래식 홈을 제거하지 않는다** — 사용자 원문: *"나의 발자취도 있어야 하니"*. 기본값은 v19.1부터 대시보드이며 클래식은 설정에서 선택.

---

## 3. 작업 파이프라인 (그대로 따를 것)
1. Python 문자열 치환 패치. **반드시** `assert s.count(old)==1` 가드.
2. `<script>` 블록 추출 → `node --check` 전수 통과 확인 (현재 **52개**).
3. **`node test_calc.js` — 계산식 회귀 테스트 4건 PASS 확인. 화면 작업이라도 반드시 돌린다.**
4. Playwright 렌더 + 48페이지 pageerror 스윕 → 0건. **dash / classic 두 모드 모두.**
5. 가로 넘침 감사 — **360px과 412px 두 폭 모두** 0건 확인.
6. 화면 변경 시: 여러 해상도 × 여러 달 실측.
7. `SendUserFile`로 전달.

### 환경
- Chromium: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` (Playwright는 `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` 로 자동 인식. `playwright install` 하지 말 것)
- 앱이 `file://`을 차단하므로 `python3 -m http.server 8913`로 서빙
- **`HOME=/root` 이다.** `~/tb` = `/root/tb`. `--directory /home/claude/...` 를 쓰면 전부 404가 난다 (실제로 겪었다)
- `pkill -f "http.server"` 는 **자기 셸까지 죽인다**(exit 144). 포트를 바꿔 새로 띄울 것
- 모달 억제 localStorage 키: `intro_hide_v141`, `entry_gate_ok`, `police_v6_accepted`, `tbook_tour_done`, `pwa_modal_closed_time`
- v19 전용 키: `tbook_home_mode` = `'dash'` | `'classic'` (미설정 = dash)
- 페이지 전환은 `showGroup(g)` → `showSubPage(id)` (GROUP_PAGES 12그룹 48페이지)
- ⚠️ **프로그램으로 `showSubPage()`를 호출하면 클릭 이벤트에 걸린 코드가 안 돈다.** `#fabAll`(전부 펼치기)이 그 예. 실제 클릭(`el.click()`)으로 검증해야 진짜 동작을 본다. 이 함정 때문에 "FAB이 0/48에서 안 뜬다"고 잘못 판단한 사례가 있다

### 작업용 스크립트 — **`tbook_tools_v19.3.zip` 로 함께 전달됨**
압축을 풀면 `RUN.md` 에 실행 순서·환경 함정·패치 규율이 전부 들어 있다. **새 세션은 이 zip 을 먼저 풀 것.** 스크립트는 전부 읽기 전용이며 앱 파일과 실기기 저장값을 바꾸지 않는다.

| 파일 | 용도 |
|---|---|
| **`test_calc.js`** | **계산식 회귀 테스트 4건 + 1~4팀 관측값 출력. 최우선** |
| `RUN.md` | 실행 안내 · 환경 함정 3개 · 패치 규율 |
| `audit_v192.js` | 48페이지 × 2모드 pageerror·넘침·넓은표·접근성 |
| `ovf360.js` | 360px / 412px 가로 넘침 감사 |
| `cone.html` | 라바콘 3중 배치 도면 시안(앱 미삽입) |
| `ftest3.js` | 전문 검색 질의 8건 + 클릭 이동 + 성능 |
| `fixtest.js` | 홈 타일 · Top8 접힘 · 검색 이동 폴백 |
| `offtest.js` | 서비스워커 등록 + 오프라인 새로고침 + 오프라인 검색 (포트 8920, `dep/` 폴더) |
| `cltest.js` | 카드 접힘 전후 페이지 높이 비교 |
| `fabtest.js` | 실제 클릭 기반 「전부 펼치기」 노출 검증 |
| `perf_clean.js` | FCP·load 측정 (9회 중간값) |
| `geom.js` | 두 버전 간 페이지 높이·노드수 대조 |

---

## 4. v19.2 → v19.3 변경분

### 4-1. 전문(본문) 검색 — 이번 판의 핵심
기존 `buildIdx()`는 **서브탭 라벨 + 카드 제목(40자 이하) + 별칭 12개**만 담았고 `runSearch()`는 `label`·`glabel`·페이지id 세 개만 검사했다. **본문 29만 자는 검색 대상이 아니었다.**

- 원본 `runSearch`는 IIFE 안의 지역 함수라 `window`로 감쌀 수 없다 → **`window.openSearch`를 감싸고 `#srch_in`에 입력 리스너를 하나 더 붙였다.** 원본이 먼저 그리고 내 리스너가 아래에 덧붙는다. **원본 코드는 한 줄도 고치지 않았다.**
- 인덱스 단위: `.card, details` (상위 단위에 이미 담긴 것은 중복 제외)
- 공백 무시 매칭(`_norm`과 동일 방식) + 문맥 스니펫 ±40자 + `<mark>` 강조
- 클릭 시: 페이지 이동 → **조상 `<details>` 전부 펼침** → 스크롤 → 2.6초 강조(`.ft-hit`)
- 대상이 아직 렌더되지 않았으면(잠금 카드 등) **보이는 가장 가까운 조상으로 폴백**
- 실측: 인덱스 구축 12ms · 검색 1.3ms · 메모리 569KB · 검색 단위 715개

| 검색어 | 개선 전 | 개선 후 |
|---|---|---|
| 150m | 0 | 6곳 |
| 109 (자살예방) | 0 | 8곳 |
| 제헌절 | 0 | 3곳 |
| 체포영장 | 0 | 2곳 |
| 그레이락 | 1 | 8곳 |
| 라바콘 | 4 | 17곳 |

### 4-2. 오프라인 — **이미 되어 있었다. 손대지 말 것**
⚠️ **중대 정정 2건. 같은 실수를 반복하지 않도록 남긴다.**

**정정 ①** — 작업 중 "오프라인이 안 된다 → `sw.js`가 없어서 404"라고 판단했다. **그것은 내 로컬 테스트 폴더에 `sw.js`를 복사하지 않아서 생긴 현상이었다.** 실제 저장소에는 `sw.js`가 오래전부터 있고 정상 작동한다. 라이브(`340patrolman.github.io/traffic-app/`)에서 확인했다. **배포 환경을 확인하지 않고 로컬 결과를 배포 상태로 단정한 오류다.**

**정정 ②** — "서비스워커 등록 1곳 vs 해제 3곳 논리 충돌"이라고 했으나 **틀렸다.** 해제 3곳은 모두 조건부이고 정상이다 — `factoryReset()`(사용자 버튼), `lock()`(KILL 만료), `?reset=1`. 충돌 없음.

**사용자의 기존 `sw.js` (캐시명 `gtw-app-v2`, 47줄) — 이것이 정답이다**
```
전략: 네트워크 우선(network-first) + fetch 시 cache:'no-store'
      → 온라인이면 항상 최신본을 받아 "수정본 자동반영" 유지
      → 200 basic 응답이면 캐시 갱신
      → 네트워크 실패 시에만 캐시 사용, 없으면 index.html 폴백
CORE = ['./', 'index.html', 'icon.png', 'robots.txt']
```
커밋 메시지에 *"sw.js no-store로 최신본 즉시 반영"* 이라고 적혀 있다 — **v1의 stale 문제를 사용자가 이미 v2에서 해결한 것이다.**

**따라서 캐시 우선(cache-first) 서비스워커를 새로 만들어 올리면 그 해결을 되돌리는 퇴보다.** 작업 중 만든 `tbook-v19.3` 캐시 우선 버전은 **폐기했고 배포하지 않았다.** 앞으로도 `sw.js`는 건드리지 않는다.

- KILL 스위치와 무충돌: 만료 시 `lock()`이 `caches`를 직접 삭제한다
- 참고: 사용자의 `CORE`에는 `manifest.json`·`icon-192/512.png`가 없다. 앱 실행에는 영향 없어 그대로 둔다(설치 메타데이터일 뿐이고 network-first라 온라인에서 항상 받는다)

### 4-3. 카드 기본 접힘 (사용자 지시)
사용자 원문: *"되도록이면 모든 탭들이 닫혀있다고 보고싶을때 열수 있도록 해야 함"*

`div.card` → `details`로 **런타임 승격**. 자식 노드를 **옮기기만** 하므로 본문·입력값·이벤트·id가 보존된다(텍스트 변경 0). `MutationObserver`로 JS 재렌더(현장대응멘트 등) 후에도 재적용.

**접지 않는 5가지 기준 (`skip()`)** — 바꾸려면 이 함수만 손대면 된다:
1. `input/select/textarea` 있는 카드 (교대근무표 설정·보고서 작성)
2. `.danger-box` 포함
3. 제목에 `🚨|긴급|죽겠다|자살|119|112|109`
4. 중첩 `details` **2개 이상** 품은 카드 = 그 화면의 색인 (서류매뉴얼·법령DB·판례). 접으면 목록 전체가 사라진다
5. 300px 미만 / 페이지에 접을 블록이 2개 미만(그 카드가 곧 페이지)

실측: 48페이지 총 높이 **127,080 → 105,128px (17% 감소)**, 카드 25개 접힘.

### 4-4. 서브탭 아이콘 체계
서브탭 47개 중 13개는 이모지가 아예 없어 이미 혼재 상태였다. **라인 SVG 27종**으로 통일(런타임 치환, 글자 라벨 유지 = 원칙 8). `currentColor` 상속으로 낮/밤·활성/비활성 자동.

- 부수효과: 기관(orgs) 그룹이 159px → 110px (한 줄 회복)
- **미결 — 사용자 결정 필요**: 서브탭이 지금 `flex-wrap:wrap!important; overflow-x:visible!important`로 강제 줄바꿈되어 있다(173줄의 원래 `overflow-x:auto`를 나중 규칙이 덮음). `.sw-ar` 좌우 화살표 어포던스는 만들어져 있으나 **죽어 있다**. 한 줄 스크롤로 바꾸면 **전 그룹 62px 고정**:

| 그룹 | 현재(줄바꿈) | 한 줄 스크롤 |
|---|---|---|
| report | 159px | 62px |
| sop | 159px | 62px |
| docs·acci·coach·ref·expr·orgs | 110px | 62px |
| **합계** | **978px** | **496px** |

  트레이드오프: 한 줄은 세로 60px/그룹 회복, 대신 탭이 화면 밖으로 밀린다. **원저자가 의도적으로 wrap을 선택한 흔적이므로 임의로 뒤집지 말 것.**

### 4-5. 기타 수정
- **홈 「현장심리」 타일 버그**: `hdGo('coach','co_excuse')` → `co_psych`. 부제 "대응멘트 · 항의" → "유형판별 · 대응원칙"
- **현장대응멘트 「⭐ 자주 쓰는 Top 8」**: 접이식(기본 접힘). 접힘 86px / 펼침 7,947px. 그 8개는 「📋 전체 상황」에도 전부 있으므로 사라지는 내용 없음
- **「▾ 전부 펼치기」 노출 기준** 3 → **2** (카드를 접어둔 뒤라 더 자주 필요)
- **표 가로 스크롤 보험**: `.tbl-scroll{overflow-x:visible}`(3708줄)이 173줄의 `auto`를 덮는 구조는 그대로 두고, **실제로 넘치는 표에만 인라인 `overflow-x:auto`**를 붙인다. CSS 규격상 `overflow-x:auto`는 `overflow-y`도 auto로 만들어 세로 삐져나옴이 잘리므로 전역 되돌림은 하지 않았다. ⚠️ **현재 넘치는 표 5개는 이미 전부 스크롤된다 — 이 결함은 활성 상태가 아니었고, 이 조치는 재발 보험이다**
- **클릭재킹 방어**: CSP `frame-ancestors`는 `<meta>`로 주면 브라우저가 무시한다(그래서 로드마다 콘솔 경고 1건). `top!==self`일 때 같은 출처면 탈출, 다른 출처면 내용을 감추는 코드를 넣었다
- **버전 표기 v19.0 → v19.3** 5곳(문서 제목·상단바 배지·발자취 소제목·푸터·정식배포판 표기). 그동안 파일은 v19.1인데 화면은 v19.0이었다
- 발자취에 「🔎 v19.3 — 찾는 앱으로」 항목 추가

### 4-7. v19.3 → v19.4 변경분 (2026-07-28)

**패치는 3건뿐이고 전부 본문 텍스트다. 로직·계산식·스크립트 무변경(블록 52개 유지).**

**① 법령DB 기준일 불일치 해소** — 앱 안에서 기준일이 두 갈래였다. 법령DB 탭만 `2026. 4.`, 나머지 26곳은 `2026.7`. 발표 자리에서 "언제 기준이냐"에 걸릴 지점이었다.
- 배지를 통째로 7월로 올리지 **않았다.** 오늘 원문 대조한 것은 별표8·별표10뿐이고, 법령DB 탭의 나머지 항목은 점검하지 않았다. 점검 안 한 것을 7월 기준이라 적으면 9장 위반이다.
- 그래서 **재확인한 범위만 병기**했다: `기준일: 2026. 4. (범칙금 별표8·별표10 : 2026. 7. 28. 원문 재확인 — 무변동)`.
- ⬜ **남은 일**: 법령DB 탭 나머지 항목을 원문 재검증하면 그때 전체를 2026.7로 통일할 수 있다.

**② 이륜차 불법 주·정차 — 과태료 부과기준 공백 반영** (📋 서류매뉴얼 탭 `page-doc_manual`, 「🏍️ 이륜차(오토바이) 단속 통합」 카드)
- 한눈표에 `불법 주·정차 / 범칙금 통고만 / 과태료 불가` 행 추가, 「5. 불법 주·정차 — 과태료 부과기준이 없다(입법 공백)」 절 신설.
- **근거 [A등급, 원문 직접 확인 2026-07-28]**: 시행령 **별표6 제6호**(승합 5만(6만)·승용 4만(5만))와 **별표7 제3호**(어린이보호구역 승합 13만(14만)·승용 12만(13만) / 노인·장애인보호구역 승합 9만(10만)·승용 8만(9만)) 어디에도 **「이륜자동차등」 금액칸이 없다.** 같은 별표의 신호위반·속도위반에는 이륜 금액이 있다 → 누락이 아니라 입법 공백. 괄호는 같은 장소 2시간 이상.
- 경찰청공고 제2026-21호가 이 공백을 메우는 안(일반 3만 / 소방시설 주변·노인장애인구역 6만 / 어린이보호구역 9만, 2시간 이상 +1만)을 예고 중이나 **미시행**이라 별도 박스로 분리하고 "현장 적용 금지"를 명시했다(원칙: 미확정치 확언 금지).
- ⚠️ **별표8의 일반 주정차 이륜 범칙금 3만원은 직접 확인하지 못했다.** 뷰어 텍스트에서 29~32호의 위반행위 열과 금액 열이 분리돼 셀 매핑이 안 된다. 예고안 원문의 "범칙금과 동일한 수준" 서술에 의한 **간접 추정(B등급)**이라 앱에는 범칙금액으로 적지 않았다. 확정하려면 별표8 HWP 원본을 봐야 한다.

**③ 버전 표기 v19.3 → v19.4** 화면 5곳(title·상단바 배지·발자취 소제목·푸터·최초실행 표기) + 발자취에 「⚖️ v19.4 — 법령 재검증」 항목 추가. 스크립트 주석의 `v19.3`은 그 판의 작업 이력이므로 그대로 뒀다.

**검증 결과 (v19.3 기준선과 대조)**

| 항목 | v19.3 | v19.4 |
|---|---|---|
| `<script>` 블록 구문오류 | 0 / 52 | **0 / 52** |
| 계산식 회귀 테스트 | 4 PASS | **4 PASS / 0 FAIL** |
| 48페이지 pageerror (dash / classic) | 2 / 2 | **2 / 2 (동일)** |
| 가로 넘침 360px / 412px | 0 / 0 | **0 / 0** |
| 넓은 표 잘림 | 0 | **0** |
| 44px 미만 터치 타겟 | 169 | **169 (동일)** |

pageerror 2건은 v19.3과 **똑같은** 것이다 — CSP `frame-ancestors` meta 무시 경고(브라우저 사양)와 **로컬에 `sw.js`를 복사하지 않아서 나는 404**. 4-2의 정정 ①과 같은 현상이니 배포 상태로 오해하지 말 것.

⚠️ **실기기 클릭 확인은 하지 못했다.** 새 내용이 DOM에 들어간 것은 확인했으나(`page-doc_manual` 안에 존재), 프로그램으로 `showSubPage()`를 호출한 결과는 3장 함정대로 신뢰할 수 없다. **사용자가 실제로 📋 서류매뉴얼 → 이륜차 카드를 열어 눈으로 확인해야 한다.**

---

### 4-6. v19.2에서 한 일 (같은 날, 참고)
- 서류매뉴얼 문서 스캔 **7장에 `loading="lazy"` + `width`/`height`**. 앱을 켤 때 헛되게 디코드하던 **비트맵 37.8MB**가 빠졌다. FCP 40~60ms 개선(load 이벤트 차이는 노이즈 범위 — 과장 금지)
- 아이콘만 있는 컨트롤 5개(`✕` 2개, 사진 추가 `+` 3개)에 `aria-label`. 앱의 「🔊 읽어주기」가 이 버튼들을 못 읽고 있었다
- 이 두 작업의 변경은 **태그 속성 12개뿐**이었고, 정규화 후 태그 diff로 확인했다

---

## 5. 계산식 (변경 금지 — 회귀 테스트로 고정됨)
```
총 근무 = Σ(근무일수 × 1회 근무시간) + 근무외가산 + 추가 − 연가/조퇴/지각
기준초과 = 총근무 − (평일수 × 8h)        ← 코드 변수명 gijun
시간외   = 기준초과 − (휴일주간일수 × 8h)  ← 코드 변수명 sigot
```
근거: 공무원수당 등에 관한 규정 §15(시간외) / §16(야간) / §17(휴일)

### 5-1. 연가 자동차감 (`lvAuto`)
```js
lvAuto = lvD*max(0, dh-8) + lvN*max(0, nh-8) + lvM*max(0, mh-8) + lvA*max(0, ah-8)
```
연가일은 **8h만 유급 인정**. 야간 연가 1일 = 13.5 − 8 = **5.5h 차감**. 3일이면 16.5h.

### 5-2. 검증 근거 [A등급]
사용자가 **① 6월 각 팀 근무지정표** 와 **② 서무가 결재 올린 표**(= 관서의 실제 지급 결정 값) 두 가지를 대조해 확정했다. 근무지정표는 계획이고 결재본은 실지급이므로, **단위 테스트보다 상위 근거**다.

### 5-3. 회귀 테스트 (`node test_calc.js`) — 4건 PASS
| 케이스 | 총근무 | 기준초과 | 시간외 |
|---|---|---|---|
| 2026-06 · 1팀 · 연가 없음 | 175h | +7h | **−17h** |
| 2026-07 · 1팀 · 연가 없음 | 200h | +24h | **0h** |
| 2026-07 · 1팀 · 야간 연가 3일 | 183.5h | +7.5h | **−16.5h** |
| 공휴일 인식 | 2026-07-17 제헌절 · 08-15 광복절 · 06-06 현충일 | | |

테스트는 새 브라우저 컨텍스트에서만 돌므로 **실기기 저장값을 건드리지 않는다.**

### 5-4. 팀별 관측값 — **미검증. 사용자 확인 필요**
`test_calc.js`가 함께 출력한다. HANDOFF v19.1에 *"2팀·4팀은 지정표 합계란이 자기 날짜칸과 불일치(자원근무·선거일 추정) — 앱은 주기 칸을 따른다"* 고 적혀 있다. **서무 결재본이 실지급 기준이라면 2·4팀에서 앱 값과 실지급이 갈릴 수 있다.** 배포본 태그가 "팀별로 수정"이므로 다른 팀 사용자에게 틀린 값이 갈 위험.

| 팀 | 2026-06 시간외 | 2026-07 시간외 |
|---|---|---|
| 1팀 | −17h (검증됨) | 0h (검증됨) |
| 2팀 | +2.5h | −3.5h |
| 3팀 | **+16h** | −5.5h |
| 4팀 | +4.5h | **+8h** |

### 5-5. §16·§17 앱 반영 상태
| 규정 | 앱 반영 |
|---|---|
| §17 휴일수당 시간외와 **병급 불가** → 차감 | ✅ `시간외 = 기준초과 − 휴일주간일수×8h` |
| §16 야간수당 시간외와 **병급 가능** → 차감 안 함 | ✅ 야간 13.5h 전액 포함 |
| §16 금액(봉급기준액 × 1/209 × 50%) | ❌ 미포함 (의도적, 원문 A등급 확정 전) |
| §17 금액(봉급기준액 × 1/26 × 150%) | ❌ 미포함 (의도적) |

**검산 [A등급]**: 경위 12,300원/h 기준 휴일수당 1일 98,873원 vs 같은 8h 시간외 98,400원 → +473원. 1/26 ≈ 8/209 라 산식상 거의 등가.

**공휴일 효과 [A등급]**: 공휴일 휴무·비번 → 기준만 −8h → 시간외 +8h 순증. 공휴일 주간근무 → 기준 −8h와 휴일주간 −8h가 상쇄 → 평일과 동일, 대신 휴일수당 1일분 별도.

---

## 6. 검증 결과 (v19.3)

| 항목 | 결과 |
|---|---|
| `<script>` 블록 구문오류 | **0 / 52** |
| **계산식 회귀 테스트** | **4 PASS / 0 FAIL** |
| 48페이지 pageerror (dash) | **0** |
| 48페이지 pageerror (classic) | **0** |
| 가로 넘침 — 412px / 360px | **0 / 0** |
| 넓은 표 잘림 | **0** (넘치는 표 5개 전부 스크롤 가능) |
| 오프라인 새로고침 | **성공** (51페이지 로드) |
| 오프라인 전문 검색 | **작동** |
| **라이브 배포 확인** | `340patrolman.github.io/traffic-app/` — 제목 **T-Book v19.3**, 홈 대시보드·하단탭 정상, 「현장심리」 타일 부제 "유형판별 · 대응원칙" 반영 확인 |
| **라이브 계산 검증** | 2026-07 1팀 실데이터: 총근무 180h / 기준 176h → 기준초과 4h → 휴일주간 3일 −24h → **−20h**. 공식과 정확히 일치 (테스트의 200h·0h는 연가 없는 깨끗한 프로필 기준이라 다르다 — 결함 아님) |

남은 콘솔 경고 1건: CSP `frame-ancestors` meta 무시 경고. 브라우저 사양이며 기능 영향 없음(방어는 4-5의 코드로 대체).

---

## 7. 남은 과제 (우선순위 순)

### 7-1. 2팀·4팀 실지급 대조 — **사용자만 가능**
5-4 표를 6월 서무 결재본과 대조. 3팀 6월 +16h, 4팀 7월 +8h처럼 1팀과 크게 다른 값이 나오므로 확인 가치가 크다.

### 7-2. 8.7 발표 — 법령 재검증 — ✅ **완료 (2026-07-28)**
**결론: 별표8·별표10 모두 무변동. 발표 자료 현행 금액 그대로 사용 가능. [신뢰도 95 / A등급]**

검증 경로 — `law.go.kr`은 WebFetch가 robots.txt로 막히므로 **Claude in Chrome으로 브라우저 직접 열람**했다. 좌표 클릭은 `lawService` iframe에 전달되지 않으니, iframe `contentDocument`에서 별표 링크의 `fncLsLawPop(seq)`를 잡아 뷰어 URL을 얻는다:
```
https://www.law.go.kr/lsBylInfoPLinkR.do?lsiSeq=286789&bylNo=0008&bylBrNo=00&bylCls=BE&bylEfYd=20260609&bylEfYdYn=Y
```
`bylNo`(0006/0007/0008/0010)와 `bylEfYd`(시행일)만 바꾸면 모든 별표·모든 판본을 직접 연다. **다음 세션은 이 URL 패턴을 그대로 쓰면 된다.**

**확인된 사실**
- 현행 시행령 = **[시행 2026. 6. 9.] 대통령령 제36388호**. HANDOFF v19.3이 적었던 "2026-02-19 제36114호"는 **공포일 기준이었고, 그 시행일은 2026-08-01(제58조만)** 이다. 별표와 무관.
- **별표8** 머리표기 `<개정 2025. 6. 2.>` — 2025.12.2 / 2026.3.19 / 2026.6.9 세 판본을 정규화 후 글자 단위 대조 → **3,201자 전부 일치, 차이 0**.
- **별표10** 머리표기 `<개정 2022. 7. 11.>` — 2025.12.2 판본과 **1,363자 전부 일치**.
- 미시행 예정분은 `[시행일: 2026. 8. 1.] 제58조` 하나뿐. 별표 무관.
- 입법예고 계류분: **경찰청공고 제2026-21호**(2026.6.19~7.29) — 개정 대상이 **안 별표6·별표7(과태료)**로 원문에 명시. 별표8·10 아님.

### 7-3. §16 야간근무수당 원문 A등급 확정
현재 **B등급**: 야간수당 = 봉급기준액 × 1/209 × 50%. `law.go.kr` 직접 접속이 robots.txt로 차단되어 2차 출처(nepla)로만 확인. 확정되면 **정산표에 야간수당 추정 행을 추가할지 먼저 물을 것**(제약 8).

### 7-4. 터치 타겟 — **미결, 사용자 결정 필요**
44px 미만 **169곳**: 현장대응멘트 73(27px 해시태그 칩) · 판례 35 · 교대근무표 15 · 현장심리 8 · 서류매뉴얼 7 · 음주 5 · 사고처리 5.
**충돌**: 44px로 키우면 한 화면 항목 수가 줄어 "한 번에 많이 보이게"와 정면 충돌. **절충안**: *누르는 것만* 44px, *읽기만 하는 라벨*은 그대로. 해시태그 칩이 실제 클릭 대상인지 확인 후 결정.

### 7-5. 서브탭 줄바꿈 vs 한 줄 스크롤 — 사용자 결정
4-4 표 참조. 978px → 496px.

### 7-6. 도면 4건 (1건 완성)
사진이 아니라 **도면**이 이해를 돕는다. 인라인 SVG면 1건 3KB 안쪽이고 `currentColor`로 낮/밤 자동 — 이모지·사진으로는 불가능하다.
- ✅ 라바콘 3중 배치(50·100·150m) — 시안 완성, 앱 미삽입
- ⬜ 시간외 계산 3단 흐름 (총근무 → 기준초과 → 시간외) ← 가장 값어치 큼
- ⬜ 음주측정 골든타임·상승기 타임라인
- ⬜ 사고현장 차량·안전삼각대 배치
- ⬜ 서류 배당 흐름 (어느 팀으로 인계)

### 7-7. 문서 스캔 7장 — `alt` 문구 + 확대 수단
`alt`가 없다. 각 이미지가 무슨 서류인지는 **사용자만 안다**(임의로 지어 쓰면 부정확한 설명이 박힌다). 그리고 라이트박스·전체화면 코드가 0건이라 1000×1416 문서를 **315px 폭으로만** 본다 — 탭하면 전체화면으로 열리는 기능이 필요.

### 7-8. 이미지 재압축은 하지 말 것 — **실측 결론**
문서 스캔 970KB(base64로 1,328KB = 파일의 56%)가 최대 감량 후보였으나, 재인코딩해보니 **품질 80은 오히려 1% 커지고 품질 72는 6%(72KB)만 줄면서 문서 글자가 상한다.** 이미 최적에 가깝다. 해상도도 적정 — 표시폭 315px × 사용자 기기 배율 2.6 = 819px 필요, 원본 1000px. **줄일 것이 없다.**

### 7-9. 구조 — 근본 문제
- `<script>` 블록 **52개**(v19.0의 48개에서 늘었다. 기존 코드를 안 건드리는 대가로 후킹 층이 쌓였다)
- `!important` **286개** — 우선순위 지도가 없어 CSS 충돌이 반복 발생(이미 2회: `.tbl-scroll`, `.nav-tabs`)
- 계산 로직이 전역에 흩어져 있다 → 한 블록으로 격리하면 회귀 테스트와 합쳐 안전망이 완성된다
- 죽은 CSS `.topbar-btns` 6세트 (DOM 개수 0 확인). **이득 수백 바이트, 리스크 비영 → 손대지 않기로 판단**

### 7-10. 시각 언어 방침 — 원칙 1의 범위를 좁혀 적을 것
본문 이모지 **1,879개**(239종, 구조적 위치만 178종), SVG는 셸·서브탭에만.
디자인 원칙 1("이모지 대신 SVG")을 무조건으로 읽고 178곳을 갈아엎으면 **작업량만 폭발하고 산만함은 그대로다.** 상용앱의 아이콘 어휘는 20~30종이다. 계층을 나눈다:
- **계층 1 셸**(홈 6타일·하단탭 5·상단바) = SVG. 완료
- **계층 2 탐색**(서브탭 47) = SVG 27종. v19.3에서 완료
- **계층 3 콘텐츠**(카드 제목 103종·본문) = **이모지 유지.** 슬랙·노션도 본문엔 이모지를 쓴다. 그리고 이 이모지는 30년 콘텐츠의 시각적 색인이다(🚨 긴급 / ⚖️ 법령 / 🇺🇸 미국 경찰학) — 지우면 스캔 속도가 떨어진다

**번호문자는 손대지 말 것 — 실증 실패.** 원문자 749곳을 타이포 배지로 바꿔봤더니, ①~⑳만 바뀌고 원문자 한글(㉮㉯㉰)·괄호형(⑴⑵)은 남아 **같은 문단에 번호 스타일이 세 가지** 공존하며 더 산만해졌다.

### 7-11. 운영
- ✅ GitHub 업로드 완료 — v19.3이 2026-07-27 라이브에 반영되어 정상 작동 중. 필요한 부속 파일(`sw.js`·`manifest.json`·`icon*.png`·`robots.txt`) 전부 저장소에 존재
- 업로드 방식은 GitHub 웹 UI **Add file → Upload files** (커밋 메시지가 전부 "Add files via upload", 244 커밋). 이 세션의 GitHub 토큰은 이 저장소에 붙어 있지 않아 API 푸시가 불가능했다(403). 파일 전달은 `SendUserFile` → 사용자가 웹 UI 업로드가 현재 유일한 경로다
- README 버전 **v19.3** 갱신
- **월간 유지보수 예약 작업** `trig_01EJkqoLdxERkspV16xqLTDZ` — cron `0 0 1 * *` UTC (매월 1일 09:00 KST), enabled, 다음 실행 **2026-08-01**. 프롬프트가 v19.0 기준이므로 v19.3 신규 항목(전문검색·sw.js·카드 접힘·서브탭 아이콘) 반영 필요
- 「부가세 납부 리마인드(집사람 사업자)」 예약 — 2026-07-25 예정이었고 이미 지났다. 발송 여부 확인 권고
- 수위표 공문 원문 대조 (현재 B등급). 내부 문서라 외부 AI 입수 불가, 사용자가 원본 스캔 확보 필요

### 7-12. 외부 AI 위임 지시문
**Grok** (실시간·속보):
```
1. 대한민국 도로교통법 시행령이 2026-02-19 대통령령 제36114호로 개정되었다.
   이 개정 및 2026년 내 다른 개정에서 별표8(범칙행위 및 범칙금액)과
   별표10 금액·항목 변동이 있었는지, 2026-08-07 이전 시행 예정인
   개정·입법예고가 있는지 최신 뉴스·공보 기준으로 알려달라.
2. 음주측정거부(도로교통법 §148조의2) 및 영장 없는 강제채혈 관련
   2025~2026년 대법원 선고·보도 판례를 사건번호와 함께 목록화해달라.
※ 각 항목에 출처 링크 필수.
```
**Gemini** (원문·대용량·이미지):
```
1. 국가법령정보센터(law.go.kr)에서 「공무원수당 등에 관한 규정」(현행)
   제15조·제16조·제17조 조문 원문을 한 글자도 바꾸지 말고 그대로 인용해달라.
   특히 §16 야간근무수당의 시간당 산정식을 확정할 것.
2. [수위표 공문 스캔본 첨부 시] 첨부 공문 원문과 전사본을 대조해
   수치·단위·조건 불일치를 전부 표로 나열해달라.
```

---

## 8. 디자인 원칙 (v19.3에서 범위 명시)
1. **이모지 대신 단색 라인 SVG — 단, 셸과 탐색(계층 1·2)에만.** 본문 이모지는 유지한다(7-10 참조). 아이콘 어휘는 20~30종을 넘기지 않는다.
2. **색은 의미로만.** 금=근무·수당 / 청=기록·문서 / 녹=대화·심리 / 주황=통제·단속 / 적=긴급·사고 / 회색=저빈도. 장식용 색 금지.
3. **색의 세기도 우선순위다.** 빈도 낮은 항목에 강한 색을 주면 눈이 그리로 끌려간다.
4. **카드 본체는 중립, 색은 좌측 4px 띠와 아이콘 배경에만.**
5. **타이포 3단계 고정**(22 / 16 / 12px), **라운드 16px**, **간격 9~10px**. 고급스러움은 색이 아니라 반복되는 규칙에서 나온다.
6. **그림자 대신 1px 경계선.**
7. **야간 모드는 순검정 금지** — `#000` 대신 `#0d1320` 계열.
8. **아이콘만 남기고 글자 라벨을 지우지 않는다.** 예뻐지지만 급할 때 못 찾는다.
9. **터치 타겟 최소 44px**(장갑 착용 시 56px 권장), 대비 4.5:1 이상.
10. **SVG는 `fill:none; stroke:currentColor`.** 색을 물려받으면 낮/밤·활성/비활성이 자동으로 맞는다. 이모지로는 불가능한 부분이고, 도면에서도 같은 원리를 쓴다.

---

## 9. 응답 규약 (사용자 개인 설정 요약)
Strategic Advisor v3.2 Pro Edition 적용. 핵심만:
- 한국어. 선결론 우선. 의례적 사과·추임새·아첨 금지.
- 신뢰도 표기 50·60·70·80·90·95 단위, 근거 A/B/C 등급 병기(분쟁 가능 시).
- **반박 의무**: 전제·논리 허점 발견 시 즉시 지적. 단 동의를 구하는 맥락에선 검증 후 합리적 인정 허용.
- **점검하지 않은 것을 점검했다고 말하지 않는다.** 이번 판에서도 두 번 정정했다 — 서비스워커 "논리 충돌"(오판), 「전부 펼치기」 0/48(측정 방법 오류).
- 정량 데이터(날짜·법조문·통계)는 **코드 실행으로 직접 검증** 후 답변. 메모리 의존 금지.
- 재산·형사·건강·가족 사안은 말미에 "전문가 교차검증 권고" 1줄 필수.
- 토큰 한도 80% 도달 시 미리 알릴 것.

---

## 10. 현재 상태 한 줄 요약
**v19.4 완성·검증·전달 완료. 계산 로직은 v18.5 이후 무변경이고 `test_calc.js` 4건으로 고정되어 있다(6월 −17h / 7월 0h / 야간연가 3일 −16.5h / 공휴일 인식). v19.4는 "법령 재검증" 판 — 8.7 발표를 앞두고 별표8·별표10을 국가법령정보센터 원문으로 세 판본 글자 단위 대조해 무변동을 확정했고(7-2 종결), 그 과정에서 찾은 이륜차 불법 주·정차 과태료 입법 공백을 별표6·7 원문 근거로 앱에 넣었으며, 법령DB 기준일 불일치를 재확인 범위 병기 방식으로 정리했다. 로직 무변경·블록 52개 유지·48페이지 × 2모드 회귀 0건. 다음은 2·4팀 실지급 대조(7-1), 터치 타겟 결정(7-4), 가드 2개 추가, 도면 4건이다.**
